import styled from "styled-components";

import { SearhCard } from "../styled";

export const StyledActorCard = styled(SearhCard)`
  .deathday {
    margin: 0;
    margin-top: 15px;
    font-weight: bold;
  }
`;
